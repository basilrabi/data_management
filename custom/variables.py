import datetime

# TODO: rename day_time to one_day
day_time = datetime.timedelta(days=1)
one_hour = datetime.timedelta(hours=1)
one_minute = datetime.timedelta(minutes=1)
zero_time = datetime.timedelta()
